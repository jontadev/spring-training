spring.datasource.url=jdbc:mysql://localhost:3306/algafood?createDatabaseIfNotExist=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=w6s7k8x3
spring.jpa.database=mysql

spring.jpa.show-sql=true

# Hibernate configuration for test porpuse
# spring.jpa.generate-ddl=true
# spring.jpa.hibernate.ddl-auto=create -- dropa e cria as tabelas - Importante para base de teste
# spring.jpa.hibernate.ddl-auto=update -- atualiza apenas o schema

# Gerar os scripts de geracao de schema
#spring.jpa.properties.javax.persistence.schema-generation.scripts.action=create
#spring.jpa.properties.javax.persistence.schema-generation.scripts.create-target=src/main/resources/ddl.sql

# Hikari
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=3
spring.datasource.hikari.idle-timeout=10000

# Flyway
spring.flyway.locations=classpath:db/migration,classpath:db/testdata
